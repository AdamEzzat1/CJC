fn sum_tail(n: i64, acc: i64) -> i64 {
    if n == 0 {
        return acc;
    }
    return sum_tail(n - 1, acc + n);
}
fn main() -> i64 {
    let result: i64 = sum_tail(100, 0);
    print(f"sum: {result}");
    result
}
